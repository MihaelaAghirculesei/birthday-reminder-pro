// Feature barrel intentionally empty.
// Import each feature directly from its own path to keep lazy-loading boundaries intact.
// e.g. import { DashboardComponent } from './dashboard/components/dashboard.component';
